b.RandomInt(first number, second number) -- generates a random whole number between the two given numbers (does not work if the second number is a negative)
b.log("value") -- logs a message in the console
